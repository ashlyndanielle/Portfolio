import React from 'react';

import Layout from '../components/layout';

const Blog = ({ pageContext }) => {
  return (
    <Layout>
      You are viewing the blog template
    </Layout>
  )
}
export default Blog;